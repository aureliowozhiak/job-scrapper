# ============================================
# Job Scrapper Pro - Environment Configuration
# ============================================
# Copy this file to .env and update values

# Application
APP_NAME=Job Scrapper Pro
APP_VERSION=3.0.0
DEBUG=false

# Server
HOST=0.0.0.0
PORT=8000
RELOAD=true

# API
API_PREFIX=/api
DOCS_URL=/api/docs
REDOC_URL=/api/redoc

# Database
# SQLite (default)
DATABASE_URL=sqlite:///./data/db/jobs.db
# PostgreSQL (optional upgrade)
# DATABASE_URL=postgresql://user:password@localhost:5432/jobscrapper

# Redis (for RQ job queue)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=yourpassword  # Optional

# Background Jobs
ENABLE_BACKGROUND_JOBS=true
JOB_TIMEOUT=3600           # 1 hour in seconds (increased for multi-source scraping)
JOB_RESULT_TTL=7200        # 2 hours in seconds

# Scraping
SCRAPING_TIMEOUT=30
MAX_RETRIES=3
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# Validation
ENABLE_PRE_VALIDATION=true
VALIDATION_SAMPLE_SIZE=100
VALIDATION_BATCH_SIZE=50

# Directories (relative to project root)
# These are created automatically if they don't exist
DATA_DIR=data
LAKE_DIR=data/lake
OUTPUT_DIR=data/output
LOGS_DIR=data/logs
DB_DIR=data/db

# Docker-specific (uncomment when running in Docker)
# REDIS_HOST=redis
# DATABASE_URL=sqlite:///./data/db/jobs.db
